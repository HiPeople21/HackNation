# Firefox MV2 Load Path

For Firefox stable, load this folder as the temporary add-on:

1. Open about:debugging#/runtime/this-firefox
2. Click "Load Temporary Add-on..."
3. Select firefox-mv2/manifest.json

This avoids MV3 service worker errors.
